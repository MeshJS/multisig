import { Wallet } from "@/types/wallet";

export default function TabGovernance({ appWallet }: { appWallet: Wallet }) {
  return (
    <main className="flex flex-1 flex-col gap-4 p-4 md:gap-8 md:p-8">gov</main>
  );
}
